import { StyleSheet } from 'react-native';

const common = StyleSheet.create({
    container: { flex: 1 },
    img: {
        height: '100%',
        width: '100%',
    },
});

export default {
    ...common,
};
